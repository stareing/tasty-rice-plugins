{
  "name": "@tasty-rice/plugins",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace for self-published browser extensions, desktop addons and small utilities surfaced on the /apps page.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "dev": "echo 'Pick a plugin: cd packages/<plugin>/ && npm run dev'",
    "type-check": "npm run --workspaces --if-present type-check",
    "lint": "npm run --workspaces --if-present lint",
    "release:pack": "node scripts/release.mjs",
    "release:local": "npm run build && npm run release:pack",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules dist-release"
  },
  "devDependencies": {
    "adm-zip": "^0.5.17",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
